name: deployery

services:
  sandbox:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${DEPLOYERY_SANDBOX_GPU_COUNT:-all}
              capabilities: [gpu]
    environment:
      DEPLOYERY_SANDBOX_GPU_COUNT: ${DEPLOYERY_SANDBOX_GPU_COUNT:-all}
